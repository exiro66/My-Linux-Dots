-- Variables globales reutilizables

local vars = {
    terminal    = "kitty",
    fileManager = "nautilus",
    browser     = "zen-browser",
    menu        = "qs ipc call launcher apps",
    wallpaper   = "qs ipc call launcher walls",
    controlCenter = "qs ipc call notch toggle",
    notifications = "qs ipc call notch open",
}

return vars
