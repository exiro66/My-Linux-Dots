general {
    lock_cmd = /home/mohamed/.config/hypr/scripts/lock.sh
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
    timeout = 600
    on-timeout = /home/mohamed/.config/hypr/scripts/lock.sh
}
